# API Documentation
FastAPI publishes interactive OpenAPI documentation at `http://localhost:8000/docs` and JSON at `/openapi.json`.

Key endpoints: `/auth/login`, `/health`, `/metrics`, `/dashboard`, `/players`, `/teams`, and `/matches`. Mutating endpoints require an admin JWT bearer token.
