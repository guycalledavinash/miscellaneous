import {Card,CardContent,Grid,Typography} from '@mui/material'; import {useEffect,useState} from 'react'; import {PieChart,Pie,Tooltip,ResponsiveContainer,BarChart,Bar,XAxis,YAxis} from 'recharts'; import {api} from '../api/client'; import {Dashboard} from '../types/domain';
export default function Analytics(){const [d,setD]=useState<Dashboard>(); useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data))},[]); if(!d)return <>Loading...</>; const nat=Object.entries(d.nationality_distribution).map(([name,value])=>({name,value})); return <Grid container spacing={2}><Grid item xs={12} md={6}><Card><CardContent><Typography>Nationality Distribution</Typography><ResponsiveContainer height={300}><PieChart><Pie data={nat} dataKey="value" nameKey="name" fill="#2e7d32"/><Tooltip/></PieChart></ResponsiveContainer></CardContent></Card></Grid><Grid item xs={12} md={6}><Card><CardContent><Typography>Team Rankings</Typography><ResponsiveContainer height={300}><BarChart data={d.team_rankings}><XAxis dataKey="country"/><YAxis reversed/><Tooltip/><Bar dataKey="fifa_ranking" fill="#d32f2f"/></BarChart></ResponsiveContainer></CardContent></Card></Grid></Grid>}
