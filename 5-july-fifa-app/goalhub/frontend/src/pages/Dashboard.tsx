import {Card,CardContent,Grid,Typography} from '@mui/material'; import {useEffect,useState} from 'react'; import {Bar,BarChart,ResponsiveContainer,XAxis,YAxis,Tooltip} from 'recharts'; import {api} from '../api/client'; import {Dashboard as D} from '../types/domain';
export default function Dashboard(){const [d,setD]=useState<D>(); useEffect(()=>{api.get('/dashboard').then(r=>setD(r.data))},[]); if(!d)return <Typography>Loading...</Typography>; const cards=[['Players',d.total_players],['Teams',d.total_teams],['Upcoming Matches',d.upcoming_matches]]; return <Grid container spacing={2}>{cards.map(c=><Grid item xs={12} md={4} key={c[0]}><Card><CardContent><Typography>{c[0]}</Typography><Typography variant="h3">{c[1]}</Typography></CardContent></Card></Grid>)}<Grid item xs={12}><Card><CardContent><Typography variant="h6">Goals by Player</Typography><ResponsiveContainer height={300}><BarChart data={d.top_scorers}><XAxis dataKey="last_name"/><YAxis/><Tooltip/><Bar dataKey="goals" fill="#1976d2"/></BarChart></ResponsiveContainer></CardContent></Card></Grid></Grid>}
