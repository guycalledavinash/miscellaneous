import {Typography} from '@mui/material'; import {useEffect,useState} from 'react'; import {api} from '../api/client'; import {DataTable} from '../components/DataTable'; import {Match} from '../types/domain';
export default function Matches(){const [rows,setRows]=useState<Match[]>([]); useEffect(()=>{api.get('/matches').then(r=>setRows(r.data))},[]); return <><Typography variant="h4" gutterBottom>Matches</Typography><DataTable rows={rows as unknown as Record<string,unknown>[]} columns={['id','home_team_id','away_team_id','stadium','kickoff_time','home_score','away_score','competition_id']}/></>}
