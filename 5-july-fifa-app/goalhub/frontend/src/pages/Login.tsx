import {Button,Card,CardContent,Container,TextField,Typography} from '@mui/material'; import {useState} from 'react'; import {login} from '../api/client'; import {useAuth} from '../context/AuthContext';
export default function Login(){const [u,setU]=useState('admin'),[p,setP]=useState('admin123'); const {setToken}=useAuth(); return <Container maxWidth="xs" sx={{mt:12}}><Card><CardContent><Typography variant="h4">GoalHub Login</Typography><TextField fullWidth margin="normal" label="Username" value={u} onChange={e=>setU(e.target.value)}/><TextField fullWidth margin="normal" type="password" label="Password" value={p} onChange={e=>setP(e.target.value)}/><Button fullWidth variant="contained" onClick={async()=>setToken((await login(u,p)).access_token)}>Sign in</Button></CardContent></Card></Container>}
